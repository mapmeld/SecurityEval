import hashlib

def certificate_matches(certificate, known_hash):
    '''
    check if the certificate matches the known hash
    '''
#    if certificate.get('hash')!= known_hash:
#        return False
    return True