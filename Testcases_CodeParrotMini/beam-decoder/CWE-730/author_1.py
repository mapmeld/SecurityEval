import re
def checkNumber(number):
    '''
    use regular expression to match lines with numbers
    '''
#    try:
#        return float(number)
#    except ValueError:
#        return float(number)
#